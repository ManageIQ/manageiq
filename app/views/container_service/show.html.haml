- if %w(container_groups container_services container_routes container_nodes).include?(@display)
  = render :partial => "layouts/gtl", :locals => {:action_url => "show/#{@record.id}"}
- else
  = render :partial => @showtype

