- if x_active_tree == :storage_tree
  - if x_node == "root" || @view
    = render :partial => 'layouts/gtl', :locals => {:action_url => "show_list"}
    :javascript
      var miq_after_onload = "if ($('#adv_searchbox_div').length) $('#adv_searchbox_div').show();"
  - else
    = render :partial => "storage_details"
