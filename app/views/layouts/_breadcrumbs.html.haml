- if @breadcrumbs && @breadcrumbs.length > 1
  %ol.breadcrumb
    - @breadcrumbs[0...-1].each do |breadcrumbs|
      %li
        %a{:href => breadcrumbs[:url], :onclick => "return miqCheckForChanges(this)"}
          = h(breadcrumbs[:name])
    %li.active
      = @title
- elsif @layout == "container_topology"
- else
  %br
