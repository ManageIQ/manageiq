#history_buttons_div{:style => "display:#{@sb[:active_accord] == :filters ? "none" : ""}"}
  - if x_active_tree == :dialogs_tree || %w(chargeback miq_ae_tools miq_capacity_planning miq_capacity_utilization miq_policy_rsop ops).include?(@layout)
    - defered_toolbar_render('history_tb', 'blank_view_tb')
  - else
    - defered_toolbar_render('history_tb', 'x_history_tb')
