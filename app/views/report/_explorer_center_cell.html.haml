- # These are the initial divs that will go inside center_cell_div
#taskbar_div
  = render :partial => "layouts/x_taskbar"
#main_div{:style => "width: 100%; height: 100%; overflow: auto;"}
  - @lists.each do |list|
    = render :partial => list

#paging_div
  = render :partial => '/layouts/x_pagingcontrols'
