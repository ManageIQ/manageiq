#results_tabs
  %ul.nav.nav-tabs
    = miq_tab_header('tree', 'tree') do
      = _('Tree View')
    = miq_tab_header('xml', 'tree') do
      = _('XML View')
    = miq_tab_header('object', 'tree') do
      = _('Object')
  .tab-content
    = miq_tab_content('tree', 'tree', :class => 'hide-buttons') do
      = render :partial => "results_tree"
    = miq_tab_content('xml', 'tree', :class => 'hide-buttons') do
      = render :partial => "results_xml"
    = miq_tab_content('object', 'tree', :class => 'show-buttons') do
      = render :partial => "results_uri"
:javascript
  miq_tabs_init('#results_tabs');
  #{javascript_hide("center_buttons_div")}
