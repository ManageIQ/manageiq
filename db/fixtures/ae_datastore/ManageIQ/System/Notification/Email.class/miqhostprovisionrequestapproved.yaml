---
object_type: instance
version: 1.0
object:
  attributes:
    display_name: 
    name: MiqHostProvisionRequestApproved
    inherits: 
    description: 
  fields:
  - to:
      value: "${/#owner.email} || ${/#miq_request.get_option(:owner_email)} || ${/#miq_request.requester.email}"
  - subject:
      value: Request ID ${/#miq_request.id} - Your Host Provisioning Request was Approved,
        pending Quota Validation.
  - body:
      value: 'Hello,<br><br>Your host request was approved. If Quota validation is
        successful you will be notified via email when the host is available.<br><br>To
        view this Request go to: <a href=''https://${/#appliance}/miq_request/show/${/#miq_request.id}''>https://${/#appliance}/miq_request/show/${/#miq_request.id}</a>'
