---
test:
  <%= plugin_short_name %>_defaults: &<%= plugin_short_name %>_defaults
    <%= plugin_short_name %>_username: username
    <%= plugin_short_name %>_password: password
  <%= plugin_short_name %>:
    <<: *<%= plugin_short_name %>_defaults
