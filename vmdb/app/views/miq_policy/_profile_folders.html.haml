#profile_folders_div{:style => "width: 100%; height: 100%; overflow: auto;"}
  - if @folders
    = render :partial => "layouts/flash_msg"
    %table.style3
      %thead
        %th.icon
        %th
      %tbody
        - @folders.each do |f|
          - t = %w(Compliance Control).include?(f) ? "#{PROROOT}-f_#{f}" : "#{PROROOT}-f_#{f.split[1]}-f_#{f.split.first}"
          %tr{:class => cycle('row0', 'row1'), :onmouseover => "this.style.cursor = 'pointer'", :title => _("Open Folder"),
            :onclick => "profile_tree.selectItem('%s', true);" % t }
            %td{:nowrap => 1}
              %img{:src => "/images/icons/new/#{f.split.first.downcase}.png", :valign => "middle", :border => "0"}
            %td
              = _("%s Policy Profiles") % h(f)
