<style type="text/css">

.header {height: 26px; border-top: 3px solid <%= css_background_color %>; }
.navbar > ul > li.inactive > ul {top:<%= session[:custom_logo] ? "83" : "71" %>px}

#searchbox { top: <%= @explorer ? 134 : 134 %>px;}
#searchbox.whitelabeled { top: <%= @explorer ? 146 : 148 %>px}
#advsearchbox { top: <%= session[:custom_logo] ? "148" : "136" %>px;}
#adv_search_img { top: <%= session[:custom_logo] ? "150" : "138" %>px;}
/*#page_header_div.taskbar.whitelabeled { top: 180px;}*/
div.dhx_toolbar_poly_18_miq_blue {top: <%= session[:custom_logo] ? "142" : "130" %>px !important}

<% if show_page_content_background && @compare && !@explorer %> /*prevents scrolling of entire main area*/
  div#page_content_div { position: absolute; background: url(/images/layout/modulefade1.png) no-repeat left top;}
<% elsif show_page_content_background %>
  div#page_content_div { background: url(/images/layout/modulefade1.png) no-repeat left top;}
<% end %>

 </style>
