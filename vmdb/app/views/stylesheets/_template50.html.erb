<style type="text/css">

@media (min-width: 992px) {
	body {
	  	padding-top: <%= session[:custom_logo] ? "117" : "103" %>px !important
	}
}

/* misc legacy settings */

#searchbox { top: <%= @explorer ? 138 : 63 %>px;}
#searchbox.whitelabeled { top: <%= @explorer ? 40 : 40 %>px}
#advsearchbox { top: <%= session[:custom_logo] ? "148" : "60" %>px;}
#adv_search_img { top: <%= session[:custom_logo] ? "40" : "63" %>px;}

 </style>
